using sc = System.Console;

namespace Revature.Week1.HelloWorld
{
  //namespaces can only have class, struct, interface, and enum in terms of scope
  class Program
  {
    private static void Main()
    {
      sc.WriteLine($"You suck {sc.ReadLine()}!");
    }
    private void UiPath()
    {

    }
  }
}